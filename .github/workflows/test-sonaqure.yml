name: SonarCloud Code Scan Only

on:
  push:
    branches: [dev]

permissions:
  contents: read

jobs:
  sonarcloud-scan:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v3

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      - name: ⬇️ Restore project
        run: dotnet restore WebApiProject/WebApiProject.csproj

      - name: 🔨 Build WebApiProject only
        run: dotnet build WebApiProject/WebApiProject.csproj --no-restore --configuration Release

      - name: 🧪 SonarCloud Scan (WebApiProject only)
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectBaseDir=./WebApiProject
            -Dsonar.sources=.
            -Dsonar.exclusions=**/bin/**,**/obj/**
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
